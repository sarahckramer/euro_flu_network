
# Check that running "network" model for a single country (no travel) produces the same results as running the 
# same inputs in "Aim 1" code
      # Note there will be differences due to lack of day-night cycle in Aim1!
